services:
  edi837ingester:
    image: edi837ingester
    build:
      context: .
      dockerfile: Edi837Ingester/Dockerfile
    depends_on:
      - sqlserver
    environment:
      # Example connection string; adjust for your app
      ConnectionStrings__DefaultConnection: "Server=sqlserver,1433;Database=MyAppDb;User ID=sa;Password=P@$$w0rf1;TrustServerCertificate=True;"

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "P@$$w0rf1"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql

volumes:
  sql_data:
