#!/usr/bin/env bash
source "$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/_payeredi_common.sh"

require_command dotnet

(cd src && dotnet csharpier format .)
(cd tests && dotnet csharpier format .)
