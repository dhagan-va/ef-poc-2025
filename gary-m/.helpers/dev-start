#!/usr/bin/env bash
source "$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/_payeredi_common.sh"

require_container_runtime

compose up -d vadb motoserver
wait_for_database
compose run --rm --no-deps db-init

printf 'PayerEDI development stack is ready.\n'
